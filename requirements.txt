# Core requirements - days 1 to 3 of the workshop.
#
# Versions are deliberately loose: the right `torch` build depends on your CUDA
# version. If you need a specific one, install torch first from
# https://pytorch.org/get-started/locally/ and then run this file.

# --- model loading and inference ---
torch>=2.4
transformers>=4.51          # Qwen3 architecture support
accelerate>=0.30
huggingface-hub>=0.24
safetensors>=0.4

# --- embeddings and retrieval (notebooks 08, 09) ---
sentence-transformers>=3.0
numpy>=1.24

# --- serving and structured output (notebooks 06, 07) ---
openai>=1.40                # the client, pointed at YOUR local server
pydantic>=2.7

# --- notebook environment ---
jupyterlab>=4.0
ipykernel>=6.29
ipywidgets>=8.1             # progress bars during downloads
